<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/medium_font"
    android:paddingLeft="@dimen/medium_font"
    android:paddingRight="@dimen/medium_font"
    android:paddingTop="@dimen/medium_font"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/welcome_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world" />
    
    <Button android:id="@+id/btnStart" 
        android:background="@drawable/custom_button"
        android:layout_width="wrap_content" 
        android:layout_height="wrap_content" 
        android:text="@string/load_timeline"
        android:layout_alignParentRight="true">     
    </Button>

    <Button
        android:id="@+id/btnLogOut"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/welcome_message"
        android:layout_below="@+id/welcome_message"
        android:layout_marginTop="16dp"
        android:text="@string/load_timeline" />

</RelativeLayout>